# 手动触发代码格式化与修复（ESLint + Prettier）

由于项目中已关闭自动格式化和自动修复功能，你需要**手动执行 ESLint 修复**和 **Prettier 格式化**。以下是完整操作指南：

---

## 1. 手动修复 ESLint 问题

适用于：自动修复可解决的 ESLint 规则问题（如未使用变量、引号风格、分号缺失、缩进错误等）。

### 操作步骤：

1. 打开一个包含 ESLint 错误或警告的文件（通常会有红色/黄色波浪线下划线提示）。
2. 按下快捷键打开命令面板：
   - **Windows / Linux**: `Ctrl + Shift + P`
   - **macOS**: `Cmd + Shift + P`
3. 在命令面板中输入：ESLint: Fix all auto-fixable problems
4. 从下拉列表中选择该命令并按回车。

✅ ESLint 将立即修复当前文件中所有**可自动修复**的问题。

> 💡 提示：此操作仅影响当前打开的文件。

---

## 2. 手动格式化代码（使用 Prettier）

适用于：统一代码风格（如缩进、换行、括号位置、尾随逗号等）。

### 方式一：通过命令面板

1. 打开你想要格式化的文件。
2. 按下快捷键打开命令面板：

- **Windows / Linux**: `Ctrl + Shift + P`
- **macOS**: `Cmd + Shift + P`

3. 输入：Format Document

✅ 代码将按照 Prettier 配置立即格式化。

> 🔄 首次选择后，VS Code 通常会记住你的选择，后续可直接使用快捷键（默认为 `Shift + Alt + F` 或 `Shift + Option + F`）快速格式化。

---

### 方式二：通过右键菜单

1. 在代码编辑区域**右键点击**。
2. 在上下文菜单中选择：
3. 如果是首次使用，选择 **Prettier - Code formatter** 作为默认格式化工具。

---

## ✅ 推荐配置（可选）

为提升体验，建议在 VS Code 的 `settings.json` 中设置 Prettier 为默认格式化器：

```json
{
  "[javascript]": {
    "editor.defaultFormatter": "esbenp.prettier-vscode"
  },
  "[typescript]": {
    "editor.defaultFormatter": "esbenp.prettier-vscode"
  },
  "[vue]": {
    "editor.defaultFormatter": "esbenp.prettier-vscode"
  }
}
```
这样无需每次选择格式化工具，直接使用快捷键即可完成格式化。

